ALTER TABLE TasksTable
ADD COLUMN taskLabelsId TEXT NOT NULL;

UPDATE TasksTable
SET taskLabelsId = '';

CREATE TABLE IF NOT EXISTS TaskLabelsTable (
id TEXT NOT NULL PRIMARY KEY,
name TEXT NOT NULL,
description TEXT NOT NULL,
colorHex TEXT NOT NULL
);